{
  "name": "glaze-manager-rules-test",
  "private": true,
  "type": "module",
  "description": "Firestore / Storage セキュリティルールの Emulator テスト。実行: firebase emulators:exec --only firestore,storage \"npm --prefix rules_test test\"",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
